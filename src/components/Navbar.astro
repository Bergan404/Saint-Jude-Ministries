---
import { Image } from 'astro:assets';
import Logo from "../assets/nav_logo.png";
---


<header id="navbar-1" class="container-fluid p-1 nav justify-content-center" role="banner">
    <div class="row justify-content-between align-items-center w-100">
        <div class="col-auto">
            <a class="navbar-logo" href="/">
                <Image src={Logo} alt="Saint Jude Logo" class="navbar-logo"/>
            </a>
        </div>
        <div class="col-auto">
            <nav class="navigation-menu d-none d-xl-flex align-items-center">
                <div class="menu-primary-container">
                    <ul id="menu-primary" class="menu">
                        <li
                            class=`${Astro.url.pathname === '/' ? 'active' : ''} menu-item menu-item-type-custom menu-item-object-custom`
                        >
                            <a href="/">Home</a>
                        </li>
                        <li
                            class=`${Astro.url.pathname === '/About' ? 'active' : ''} menu-item menu-item-type-custom menu-item-object-custom`
                        >
                            <a href="/About">About</a>
                        </li>
                        <li
                            class=`${Astro.url.pathname === '/Documents' ? 'active' : ''} menu-item menu-item-type-custom menu-item-object-custom`
                        >
                            <a href="/Documents">Documents</a>
                        </li>
                        <li
                            class=`${Astro.url.pathname === '/Contact' ? 'active' : ''} menu-item menu-item-type-custom menu-item-object-custom`
                        >
                            <a href="/Contact">Contact</a>
                        </li>
                    </ul>
                </div>
            </nav>
            <div
                id="burger-nav"
                role="button"
                aria-label="Open navigation"
                class="d-xl-none"
            >
                <span></span>
                <span></span>
                <span></span>
                <span></span>
            </div>
            <div class="burger-overlay"></div>
        </div>
    </div>
    <nav class="mobile-nav d-flex justify-content-center align-items-center">
        <div class="menu-primary-container">
            <ul id="menu-primary" class="menu">
                <li
                    class="menu-item menu-item-type-custom menu-item-object-custom"
                >
                    <a href="/">Home</a>
                </li>
                <li
                    class="menu-item menu-item-type-custom menu-item-object-custom"
                >
                    <a href="/About">About</a>
                </li>
                <li
                    class="menu-item menu-item-type-custom menu-item-object-custom"
                >
                    <a href="/Documents">Documents</a>
                </li>
                <li
                    class="menu-item menu-item-type-custom menu-item-object-custom"
                >
                    <a href="/Contact">contact</a>
                </li>
            </ul>
        </div>
    </nav>
</header>

<style lang="scss" is:global>
    .nav {
        position: fixed;
        // position: relative;
        z-index: 1000;
        width: 100%;
        left: 0;
        right: 0;
        top: 0;
        margin: auto;
        text-align: center;
        background-color: white;

        .navbar-logo {

            img {
                display: inline-block;
                max-height: 59px;
                max-width: fit-content;
                padding: 7px 4px 6px 15px;
                vertical-align: middle;
            }
        }

        .active {
            // border-bottom: 2px solid #C2AA95;

            a {
                color: black !important;
                font-weight: bold;
            }
        }

        .navigation-menu {
            .menu {
                list-style: none;
                margin: 0px;
                padding: 0px;

                .menu-item {
                    margin: 0px 25px 0px 0px;
                    display: inline-block;

                    a {
                        color: rgba(0, 0, 0, 0.726);
                        text-decoration: none;
                        transition: all 0.2s ease-in-out;
                        font-family: Lora, serif;
                        font-size: 14pt;

                        &:hover {
                            color: #C2AA95;
                        }
                    }
                }
            }
        }

        .mobile-nav {
            background-color: black;
            position: fixed;
            z-index: 99;
            top: 0;
            left: 0;
            height: 100vh;
            max-width: 300px;
            width: 100%;
            padding: 80px 30px 50px 20px;
            overflow-y: auto;
            transform: translateX(-100vw);
            transition: transform 0.55s cubic-bezier(0.785, 0.135, 0.15, 0.86);

            &.open {
                transform: translateX(0px);
            }

            .menu {
                list-style: none;
                margin: 0px;
                padding: 0px;

                .menu-item {
                    margin: 15px 0px;

                    a {
                        color: white;
                        text-decoration: none;
                        text-transform: uppercase;
                        font-weight: 700;
                        font-size: 1em;
                    }
                }
            }
        }

        /*  OVERLAY
========================================== */
        .burger-overlay {
            position: fixed;
            top: 0;
            left: 0;
            z-index: 1;
            width: 100%;
            height: 100%;
            -webkit-transition: all 0.3s ease-in-out;
            -moz-transition: all 0.3s ease-in-out;
            -o-transition: all 0.3s ease-in-out;
            transition: all 0.3s ease-in-out;
            background-color: #000000;
            opacity: 0;
            visibility: hidden;
        }

        .burger-overlay.show {
            opacity: 0.5;
            visibility: visible;
        }

        #burger-nav {
            width: 40px;
            height: 30px;
            position: relative;
            transform: rotate(0deg);
            transition: 0.5s ease-in-out;
            cursor: pointer;
            z-index: 999;
            margin-left: auto;
            margin-right: 1em;


            &.open {
                span {
                    background-color: white;
                }
            }

            span {
                display: block;
                position: absolute;
                height: 3px;
                width: 100%;
                background-color: white;
                opacity: 1;
                left: 0;
                transform: rotate(0deg);
                transition: 0.25s ease-in-out;
            }
        }

        #burger-nav span:nth-child(1) {
            top: 0px;
        }

        #burger-nav span:nth-child(2),
        #burger-nav span:nth-child(3) {
            top: 10px;
        }

        #burger-nav span:nth-child(4) {
            top: 20px;
        }

        #burger-nav.open span:nth-child(1) {
            top: 18px;
            width: 0%;
            left: 50%;
        }

        #burger-nav.open span:nth-child(2) {
            transform: rotate(45deg);
        }

        #burger-nav.open span:nth-child(3) {
            transform: rotate(-45deg);
        }

        #burger-nav.open span:nth-child(4) {
            top: 18px;
            width: 0%;
            left: 50%;
        }
    }
</style>